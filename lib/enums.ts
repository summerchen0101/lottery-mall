export enum DateRangeType {
  Today = 1,
  Yesterday = 2,
  ThisWeek = 3,
  LastWeek = 4,
  ThisMonth = 5,
  LastMonth = 6,
  Tomorrow = 7,
  inYear = 8,
}

export enum BankCardStatus {
  On = 1,
  Off = 2,
}

export enum RechargeStatus {
  Success = 1,
  Fail = 2,
}
