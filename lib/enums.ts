export enum ProcessStatus {
  Pending = 1,
  Running = 2,
  Finish = 3,
  Cancel = 4,
}

export enum AccountingStatus {
  Pending = 1,
  Running = 2,
  Finish = 3,
  Cancel = 4,
}

export enum NewsType {
  System = 1,
  Game = 2,
  Activity = 3,
}

export enum Section {
  Full = 'F',
  FirstHalf = 'FH',
}
